# secrets
.env

# local-only prompt overrides (KKR product naming) — MUST never be committed
*.local.ts
*.local.prompt

# dependencies
node_modules/

# pipeline cache (re-downloadable; large)
data/

# generated output is reproducible — except the committed sample snapshot
output/*
!output/sample/
